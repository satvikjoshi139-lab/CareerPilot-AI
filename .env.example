# Vite requires the VITE_ prefix for browser-side env vars.
# The vite.config.js also auto-bridges GEMINI_API_KEY → VITE_GEMINI_API_KEY,
# so either form works.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
